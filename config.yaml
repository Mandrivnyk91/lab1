binds:
- port: 3000
  listeners:
  - routes:
    - backends:
      - ai:
          provider:
            gemini:
              model: models/gemini-2.5-flash-lite
          name: gemini
      policies:
        backendAuth:
          key: "$GEMINI_API_KEY"
